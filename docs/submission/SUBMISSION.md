# Чек-лист пакета сдачи — «Ариадна» (дедлайн 04.07.2026, 23:59)

Соответствие пункту «Пакет сдачи» из `docs/dev/TASK.md`. Заполняется/довводится
оркестратором/PM по мере готовности каждого пункта — этот файл фиксирует состав
и статус, не сам контент.

## 1. Ссылка на код в VCS

**Статус: готово (04.07 ~20:50).** Ветка `main` запушена на GitHub с сервера
по SSH-ключу (публичная часть добавлена PM в аккаунт; после сдачи ключ можно
отозвать: Settings → SSH and GPG keys).

Ссылка: **https://github.com/Dikoper/ARIADNA-NEO4j-RAG**

## 2. Облачный диск: архив кода + видео-демо

**Статус: скрипт архива готов (`scripts/build_submission_archive.sh`), видео —
ждёт записи по `docs/submission/DEMO-SCRIPT.md`.**

- Архив кода: `./scripts/build_submission_archive.sh` → `artifacts/ariadna-submission.tar.gz`
  (см. докстринг скрипта — что именно входит/не входит; `artifacts/` уже в
  `.gitignore`, архив туда не попадёт в git случайно).
- Видео-демо: записывается вручную PM/оператором по сценарию
  `docs/submission/DEMO-SCRIPT.md` (~5–7 минут, потактовый план с репликами).
  Готовый файл положить рядом, например `docs/submission/demo-video.mp4`
  (в git не коммитить — большой бинарник; путь на диске зафиксировать здесь
  после записи).
- Ссылка на облачный диск с обоими файлами: **https://disk.yandex.ru/d/Vo0tJWsdzO_sIg**
  Для отправки собирается и zip-вариант архива (решение PM 04.07):
  `artifacts/ariadna-submission.zip` — то же содержимое (`git archive HEAD`),
  другой формат упаковки. Видео PM заливает в ту же папку со своего компьютера.

## 3. Презентация (.pptx/.pdf)

**Статус: готова (reviewer APPROVE 04.07).**

Файлы: `docs/submission/presentation/ariadna.pptx` и `ariadna.pdf` (12 слайдов).
Генератор — `docs/submission/presentation/build_pptx.py`: текст слайдов вынесен
в структуру `SLIDES` в начале файла; после правки перезапустить скрипт
(`.venv/bin/python docs/submission/presentation/build_pptx.py`). На слайде 12 —
ссылки на папку облака (видео и материалы) и на GitHub-репозиторий
(вписаны 04.07 ~20:50, презентация пересобрана).

## 4. Ссылка на развёрнутое решение (желательно)

**Статус: честно — только локальное демо.**

Решение развёрнуто на DGX Spark (локальное железо, GB10/Blackwell) без
публичного внешнего адреса — вынести наружу без риска для приватности данных
корпуса и с учётом дедлайна не пробовали. Публичной ссылки на развёрнутое
решение нет; вместо неё — видео-демо (см. пункт 2) и воспроизводимый локальный
запуск (см. `README.md`, разделы «Демо за 3 шага» и «Быстрый старт»).

Если PM решит развернуть публично (например, туннель/облачная VM) —
вписать ссылку здесь: `<< ССЫЛКА, ЕСЛИ ПОЯВИТСЯ >>`

## Сводка готовности

| # | Пункт | Статус |
|---|---|---|
| 1 | Ссылка на код в VCS | ✅ github.com/Dikoper/ARIADNA-NEO4j-RAG (main запушен) |
| 2 | Архив кода | ✅ ariadna-submission.zip (+tar.gz), собран от финального коммита |
| 2 | Видео-демо | ⛔ ждёт записи (сценарий готов); заливка в папку диска — PM |
| 3 | Презентация | ✅ ariadna.pptx + ariadna.pdf, ссылки на слайде 12 вписаны |
| 4 | Ссылка на развёрнутое решение | 💤 нет (желательный пункт, честно закрыт видео) |
