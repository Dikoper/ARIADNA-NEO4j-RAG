# ERRORS.md — реестр кодов ошибок

Правило: новый путь ошибки в коде = новая строка здесь, тем же пакетом изменений.
Формат события ERROR в логе — см. CONVENTIONS.md §4 (обязателен контекст
для воспроизведения). Диагностика по коду/doc_id — `scripts/diagnose.py` (задача A-17).

Диапазоны: `INGEST-0xx` конвертация/чанкинг · `EXTRACT-0xx` извлечение ·
`GRAPH-0xx` загрузка/Cypher · `SEARCH-0xx` роутер/ответы · `UI-0xx` интерфейс ·
`DEPLOY-0xx` инфраструктура.

| Код | Симптом | Вероятная причина | Где чинить |
|---|---|---|---|
| INGEST-001 | Файл не сконвертирован в текст | Битый/зашифрованный PDF, неподдержанный формат | `ingest/convert` — обработчик формата; файл в skip-лист с логом |
| INGEST-002 | Пустой или мусорный текст (<200 знаков с документа) | PDF без текстового слоя (скан) или колонтитулы «съели» текст | `ingest/normalize`; сканы — в skip-лист (OCR вне скоупа) |
| INGEST-003 | По эталонной теме жюри 0 документов в targets.jsonl | В ядре корпуса нет документов с ключевыми словами темы (дыра в корпусе) | `ingest/select`; проверить корпус вручную/расширить словарь синонимов темы |
| INGEST-004 | Каталожная карточка получила 0 файлов в собственной области после разбиения по границам вложенных карточек | Ошибка в алгоритме схлопывания (`_collapse_to_card_dir`)/разграничения (`_scan_own`) — теоретически не должна происходить | `ingest/catalog` — карточка пропускается + WARN, не блокирует прогон |
| INGEST-005 | Эмбеддинг каталожных карточек не получен | Ollama (`/api/embed`) недоступна/битый ответ — 2 попытки батчем целиком | `ingest/catalog` — карточки пишутся в catalog.jsonl с embedding=null, панель рекомендаций деградирует до текстового списка |
| EXTRACT-001 | Ответ LLM не парсится в ExtractionResult | Модель нарушила JSON-схему | `extraction/llm` — ретрай с уточнением; после 2 неудач — чанк в skip-лист |
| EXTRACT-002 | Неизвестная единица измерения | Единицы нет в таблице нормализатора | `extraction/rules` — добавить единицу в таблицу + тест |
| EXTRACT-003 | Связь LLM отброшена | source/target связи не входят в список сущностей чанка (после канонизации имён) | `extraction/postprocess` — не блокирует чанк, только саму связь; лог WARNING с chunk_id/типом/именами |
| EXTRACT-004 | Ollama chat (`/api/chat`) недоступна при извлечении | Сеть/контейнер недоступен, таймаут, битый JSON транспорта | `extraction/llm_extract` — ретрай (RETRY_ATTEMPTS), затем чанк в `extract_skiplist.jsonl` |
| GRAPH-001 | Узел/связь не загружены в Neo4j | Нарушение констрейнта (дубликат id, null у обязательного поля) | `graph/loader` — валидация по contracts до записи |
| GRAPH-002 | Chunk загружен в Neo4j без вектора embedding | chunks-файл ещё без эмбеддингов (A-04 не завершён) или эмбеддинг конкретного чанка пуст | `graph/lexical_loader` — не блокирует загрузку; догрузить вектор при повторном запуске после A-04 |
| GRAPH-003 | Словарь `ontology/synonyms.yaml` не прошёл валидацию | Поле `type` записи не входит в `contracts.EntityType` (опечатка/новый тип без согласования) | `graph/ontology` — `load_synonyms()` бросает исключение до использования словаря; чинить запись в `synonyms.yaml` |
| GRAPH-004 | Связь ExtractionResult пропущена при загрузке сущностного графа | source/target связи (после канонизации имени) не совпадает ни с одной сущностью того же чанка — рассинхрон извлечения LLM (редко после EXTRACT-003) | `graph/entity_loader` — WARN с chunk_id/типом/именами, связь пропускается, чанк и остальные связи/сущности грузятся дальше |
| GRAPH-005 | Неоднозначный резолв конца связи внутри чанка | Две разные сущности одного чанка совпадают по имени (регистронезависимо), но различаются по EntityType — Relation несёт только имя, тип конца неизвестен | `graph/entity_dedup._resolve_endpoint` — WARN с chunk_id/именем/кандидатами, берётся первый по порядку появления, связь НЕ теряется (в отличие от GRAPH-004) |
| SEARCH-001 | Роутер не подобрал шаблон запроса | Вопрос вне покрытия шаблонов | `search/router` — fallback на векторный RAG-ответ + лог вопроса |
| SEARCH-002 | Слот шаблона Cypher не заполнен | LLM не извлекла параметр из вопроса | `search/router` — переспрос/дефолт; лог промпт-хеша и ответа |
| SEARCH-003 | Ollama `/api/embed` вернула пустой/неполный/не-JSON ответ или недоступна | Сеть/контейнер недоступен, битый ответ, несовпадение размерности вектора | `search/embeddings` — ретрай батча (RETRY_ATTEMPTS), затем изоляция по одному чанку; сбойный чанк — в `embed_skipped.jsonl` |
| SEARCH-004 | Векторный поиск в Neo4j не выполнился | Neo4j недоступен, индекс `chunk_embedding_idx` отсутствует/ещё не создан, битый вектор запроса | `search/rag_demo` — Answer(found=False, «в корпусе не найдено»); проверить `graph/lexical_loader` self_check (vector_index_exists) |
| SEARCH-005 | Answer-LLM (ANSWER_MODEL через Ollama) недоступна или вернула пустой content | Сеть/контейнер недоступен, битый ответ, весь бюджет max_tokens ушёл на thinking (content пуст, есть только reasoning) | `search/rag_demo` — увеличить ANSWER_MAX_TOKENS; деградация — текст с найденными citations без синтеза |
| DEPLOY-001 | Сервис (Neo4j/Ollama) недоступен | Контейнер не поднят / порт занят | `deploy/` — healthcheck в compose; README-инструкция |
