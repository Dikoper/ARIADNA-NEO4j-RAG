# Worklog: extraction

Шаблон записи (≤ 8 строк). Эскалация — блок `⛔ ЭСКАЛАЦИЯ:` (симптом, что пробовал, гипотезы).

```markdown
## ГГГГ-ММ-ДД ЧЧ:ММ · <агент> · <ID задачи>
**Сделано:** …
**Решения:** …
**Проблемы:** …
**Открыто:** …
```

---

## 2026-07-03 20:50 · module-dev (Sonnet) · A-07
**Сделано:** `src/ariadna/extraction/__init__.py`, `units.py` (таблицы UNIT_TABLE/
OPERATOR_RULES/PARAM_KEYWORDS + скомпилированные regex NUMBER_RE_STR/RANGE_*/SINGLE_RE),
`rules.py` (`extract_constraints(text) -> list[NumericConstraint]`, чистая функция,
только re). Покрыты единицы мг/л, мг/дм³, г/л, г/дм³, °C/градусы, м³/ч, м3/ч, т/сут, %;
операторы LE/LT/GE/GT/EQ/RANGE (слова + явные знаки <,>,≤,≥); числа с запятой/точкой
и разрядами через пробел/НБП; диапазоны дефис/тире/от-до с приоритетом над одиночными.
**Решения:** канон концентраций — мг/л (г/л, г/дм³ ×1000; мг/дм³ = мг/л ×1; прочие
единицы канон = унифицированное написание). Для RANGE `value`/`value_max` — сырые
(как в тексте), `norm_value` — только нижняя граница в каноне (в contracts.py нет
norm_value_max) — потребитель A-10 при необходимости конвертирует value_max тем же
множителем unit→norm_unit. Constraint эмиттится только при найденной рядом единице
(без единицы `norm_unit` нечем заполнить осмысленно) — «голые» числа (годы, номера)
игнорируются. Оператор/параметр — эвристика по ближайшей слева фразе в окне
30/60 симв., обрезаемом по границе предложения; без оператора — EQ по умолчанию;
без параметра — `param=""` (constraint всё равно возвращается).
**Проблемы:** нет.
**Открыто:** param для перечислений («сульфаты, хлориды, Ca, Mg, Na по 200–300 мг/л»)
берёт ближайшее слово (Na) — не разворачивает список; параметр справа от числа
(«от 200 до 300 мг/л сульфатов») не подхватывается (эвристика смотрит только влево);
отрицательные числа не поддержаны (минус неотличим от тире диапазона). Примеры
вход→выход (смоук): «минерализация до 10 000 мг/л» → param=минерализация, LE,
10000.0 мг/л; «температура не ниже 60 °C» → param=температура, GE, 60.0 °C;
«скорость потока 200–300 м³/ч» → param=скорость потока, RANGE, 200.0..300.0 м³/ч.

## 2026-07-03 21:40 · module-tester (Sonnet) · A-07
**Сделано:** `tests/extraction/test_rules.py` — 60 тестов (57 + 3 xfail) на
`extract_constraints`. Приёмочный минимум: числовые подстроки и полные тексты
всех 4 эталонных запросов жюри (диапазон 200–300 мг/л, ≤1000 мг/дм³, «5 лет» без
единицы → []). Далее: канонизация единиц (мг/л, мг/дм³, г/л×1000, г/дм³×1000, °C/
градусы/кириллич. «С», м³/ч, м3/ч, т/сут, %/процентов) параметризованно; все
словесные операторы + отрицания (не ниже→GE, не выше→LE) + явные знаки; диапазоны
дефис/тире/от-до без дублирования как одиночных; десятичная запятая/точка, тысячи
с пробелом/НБП; параметр слева / не найден → ""; отрицательные случаи (голые числа,
неизвестная единица «атм», пустая строка, текст без чисел); идемпотентность,
отсутствие file I/O (мок `builtins.open`), валидность pydantic + непустой
source_text. Известные границы (worklog выше «Открыто») — 3 xfail(strict=True):
перечисление параметров, параметр справа, отрицательные числа.
**Найденные баги:** нет — все проверки (включая канонизацию единиц из паспорта)
совпали с решениями из worklog A-07 module-dev без расхождений.
**Проблемы:** нет.
**Открыто:** `tests/extraction/ -q` — 57 passed, 3 xfailed (0.12s); полный
`tests/ -q` — 219 passed, 3 xfailed (128s); `lint_precomments.py` — ок.
Вердикт: ПРОШЁЛ.

## 2026-07-03 23:20 · module-dev (Sonnet) · A-08
**Сделано:** `extraction/prompt.py` (SYSTEM_PROMPT — 8 EntityType+6 RelationType
из contracts.py, build_user_prompt), `extraction/ollama_client.py` (native
`/api/chat`, `think:false`, парсинг ```json-обёртки + валидация `_RawExtraction`
поверх contracts.Entity/Relation, `clean_env_var`), `extraction/postprocess.py`
(`canonicalize_entities` — `graph.ontology.canonical_name` + слияние дублей,
`filter_relations` — отбраковка source/target вне сущностей чанка), `extraction/
llm_extract.py` (`extract_chunk` — retry-цикл, `run_extraction_batch` — CLI
`--limit/--targets/--workers`, ThreadPoolExecutor, рестарт по chunk_id, skip-лист).
Новые коды `EXTRACT-003`/`EXTRACT-004` в ERRORS.md.
**Решения:** `prompt_hash` = sha256(SYSTEM_PROMPT)[:12] — константа промпт-версии,
не за-хэш конкретного чанка. LLM отдаёт только `{entities, relations}` —
constraints/doc_id/chunk_id/model/prompt_hash проставляет код. Файл >350 строк
→ вынес HTTP-клиент в отдельный `ollama_client.py`.
**Проблемы:** `.env`/`.env.example` — инлайн-комментарий на строке
`EXTRACTION_MODEL=...  # ...` ломал вызов (HTTP 400): общий `_load_dotenv`
(search/embeddings) не срезает `# ...`. Поправил обе строки в `.env`/
`.env.example` (комментарий вынесен строкой выше, п.9 задания) + защитная
`clean_env_var()` в ollama_client.py на случай будущих правок .env.
**Открыто:** живой smoke `--limit 2` на реальной Ollama — 2/2 успешно
(83.9 с/2 чанка ≈ 42 с/чанк с холодным стартом контекста ретраев; логи
`logs/pipeline/extract_20260703T231817.jsonl`); мок-сценарии (`python -c`,
не тест-файл) проверили: ретрай битого JSON → успех, 2×битый JSON → skip
EXTRACT-001, 2×сетевой сбой → skip EXTRACT-004, связь на несуществующую
сущность → отброшена (EXTRACT-003 WARNING). `extracted.jsonl`/
`extract_skiplist.jsonl` содержат только эти 2 смоук-чанка — массовый прогон
по ядру НЕ запускал (по заданию).

## 2026-07-04 00:10 · module-tester (Sonnet) · A-08
**Сделано:** `tests/extraction/{test_ollama_client,test_prompt,test_postprocess,
test_llm_extract}.py` — 61 новый тест (offline, HTTP-слой мокается подменой
`_NO_PROXY_OPENER.open`/`call_extraction_llm`, без Ollama) + 1 живой смоук
`extract_chunk` (skip если Ollama недоступна — прошёл на живом стенде).
Покрыто: контракт ExtractionResult (doc_id/chunk_id/model/prompt_hash — код,
не LLM; constraints — rules.extract_constraints, не LLM; LLM-«лишние» поля
игнорируются схемой); ретрай схемы →EXTRACT-001, сетевой сбой →EXTRACT-004
(тем же промптом, не ретрай-подсказкой); EXTRACT-003 при отбросе связи;
```json-обёртка, пустые списки, смешанный RU/EN + канонизация синонимов
(electrowinning→электроэкстракция, Copper→медь), слияние дублей после
канонизации; batch: resume по chunk_id, no-op на готовом выводе, изоляция
сбоя одного чанка (skiplist ≠ падение прогона), --targets/--limit;
`clean_env_var` (инлайн-комментарий/хвостовые пробелы/отсутствующая переменная).
**Найденные баги:** нет — расхождений с контрактами/паспортом не найдено.
**Проблемы:** нет.
**Открыто:** `tests/extraction/ -v` — 118 passed, 3 xfailed (4.4s, живой смоук
включён и прошёл — Ollama была доступна на стенде); полный `tests/ -q` —
280 passed, 3 xfailed (67s); `lint_precomments.py` — ок. Вердикт: ПРОШЁЛ.
