# Worklog: search

Шаблон записи (≤ 8 строк). Эскалация — блок `⛔ ЭСКАЛАЦИЯ:` (симптом, что пробовал, гипотезы).

```markdown
## ГГГГ-ММ-ДД ЧЧ:ММ · <агент> · <ID задачи>
**Сделано:** …
**Решения:** …
**Проблемы:** …
**Открыто:** …
```

---

## 2026-07-03 20:10 · module-dev · A-04
**Сделано:** `search/embeddings.py` + `__init__.py`: `embed_texts()` (Ollama `/api/embed`, батч, без файлов — переиспользуется онлайн) и офлайн-батч `run_embedding_batch()`. Живой прогон: 9580/9580 векторов, dim=1024, 1207 с (~7.9 чанк/с), 0 пропусков; валидация выхода — все записи парсятся в Chunk, размерность едина, покрытие 1:1 с chunks.jsonl.
**Решения:** только stdlib (urllib), .env без python-dotenv; батч 32 чанка (замер ~1 с/батч на прогретой модели); перезапуск по chunk_id из выходного файла; ретрай батча ×2, затем изоляция по чанку → `embed_skipped.jsonl`; новый код SEARCH-003 в ERRORS.md; запросы к локальной Ollama идут мимо системного HTTP(S)_PROXY (иначе 502).
**Проблемы:** нет.
**Открыто:** загрузка векторов в Neo4j vector index — зона graph; онлайн-путь вызывает `embed_texts()` напрямую.

## 2026-07-03 20:40 · module-tester · A-04
**Сделано:** `tests/search/` (conftest.py + test_embeddings.py), 12 тестов: контракт выхода (Chunk, поля байт-в-байт, единая размерность), перезапускаемость (частичный/полный/повторный прогон, без дублей и без лишних вызовов embed_texts), изоляция сбоя (мёртвый порт → SEARCH-003 в лог + embed_skipped.jsonl, выход не портится), `embed_texts` (пустой вход, мёртвый хост, битый JSON, несовпадение числа векторов), живой смоук (dim=1024, разные тексты → разные векторы) и обход HTTP_PROXY (env на мёртвый адрес — живой запрос всё равно проходит), выборочный смоук реального `chunks_embedded.jsonl` (первая/последняя/100 случайных строк, seed=42).
**Решения:** офлайн-тесты — monkeypatch `embeddings.embed_texts` детерминированной заглушкой (dim=4); изоляция сбоя и обход прокси — реальный сетевой вызов на мёртвый порт `127.0.0.1:1` (ConnectionRefused мгновенно, таймаут не нужен); проверка живой Ollama через свой no-proxy opener в conftest (`OLLAMA_LIVE`) — живые тесты скипаются, а не падают, если стенд не поднят.
**Найденные баги:** нет (логика соответствует контракту и паспорту).
**Открыто/наблюдение:** `get_logger`/`LOG_DIR` — жёстко `logs/pipeline/` относительно cwd, не параметризуется через `run_embedding_batch()`; тесты с реальным вызовом пишут `test_a04_*.jsonl` в боевой `logs/pipeline/` (каталог в `.gitignore`, не коммитится, но замусоривает рабочую директорию при локальном прогоне) — не баг A-04, общее свойство logutil (A-02), к сведению.
**Вердикт:** ПРОШЁЛ. 12/12 тестов зелёные (`.venv/bin/python -m pytest tests/search/ -v`), `lint_precomments.py` — ок.

## 2026-07-03 21:00 · module-dev · M-01
**Сделано:** `search/rag_demo.py` (297 строк) — сквозной путь «вопрос → ответ с цитатами» без роутера: `embed_texts()` (переиспользован, не продублирован) → `vector_search_chunks()` (bolt, `CALL db.index.vector.queryNodes('chunk_embedding_idx', …)` + `(:Document)-[:HAS_CHUNK]->(:Chunk)`, только чтение) → `call_answer_llm()` (Ollama `/v1/chat/completions`, ANSWER_MODEL) → `answer_question()` собирает `contracts.Answer`, citations строит код из найденных чанков (doc_id/chunk_id/title/year/quote≤300). CLI: `python -m ariadna.search.rag_demo "вопрос" [--top-k] [--json]`.
**Решения:** живой стенд показал, что Ollama отдаёт thinking в отдельном поле `message.reasoning`, а не в `<think>` внутри `content` — но код всё равно страхуется regex-вырезкой `<think>…</think>` из content на случай другой версии/конфигурации; ANSWER_MAX_TOKENS=8192 (замер: тривиальный вопрос уже потратил ~1500 ток. на thinking); свой no-proxy opener (тот же факт про HTTP_PROXY→502, что и в embeddings.py); нет чанков → found=False «в корпусе не найдено» (инвариант №6); LLM-синтез упал, но чанки есть → found=True с честным текстом ошибки + реальные citations (цитаты не теряются). Новые коды SEARCH-004 (векторный поиск), SEARCH-005 (answer-LLM/пустой content) в ERRORS.md.
**Проблемы:** нет — импорт, lint, живой смоук прошли с первого прогона.
**Живой смоук:** эталонный вопрос жюри №2 (циркуляция католита при электроэкстракции никеля) → осмысленный русский ответ со ссылками [1]…[8] на фрагменты + честная оговорка «универсальной оптимальной скорости в источниках нет»; 8 реальных citations (doc_id/chunk_id/title/year/quote) из ИТС НДТ 12-2016 и Обзора техрешений 2024/2025; лог `logs/pipeline/rag_20260703T205513.jsonl` — query_received → answer_ready.
**Открыто:** это будущий `rag_fallback` для роутера A-10 (интерфейс `answer_question(question, top_k)` уже пригоден для вызова из router/retrieval); синтез с reasoning-моделью занимает ~2–3 мин на вопрос — для A-11 стоит обсудить бюджет/таймаут перед демо жюри.

## 2026-07-03 21:30 · module-tester · M-01
**Сделано:** `tests/search/test_rag_demo.py`, 15 тестов + расширение `conftest.py` (добавлен `NEO4J_LIVE` рядом с `OLLAMA_LIVE`, тот же приём — bolt-коннект в обход прокси, скип а не падение). Покрыто: контракт Answer/citations (собраны кодом, порядок = порядку чанков, усечение quote≤300 по границе слова, в т.ч. текст без пробелов), инвариант №6 (0 чанков → found=False, «в корпусе не найдено», LLM не вызывается), деградация синтеза (AnswerLLMError, чанки есть → found=True, citations на месте, честный текст, SEARCH-005 в логе), недоступность Neo4j (VectorSearchError оборачивает любое исключение драйвера, driver.close() вызывается всегда через finally, SEARCH-004 в логе, без трейсбека наружу) + попутно сбой эмбеддинга вопроса (found=False), парсинг reasoning-модели (`<think>` в content вырезается регистронезависимо/многострочно; отдельное поле `message.reasoning` игнорируется при непустом content; пустой content → AnswerLLMError + SEARCH-005), обход HTTP_PROXY (офлайн — локальный `ThreadingHTTPServer` вместо живой Ollama, мёртвый прокси в env, запрос всё равно доходит), живой смоук (`OLLAMA_LIVE and NEO4J_LIVE` → короткий вопрос, found=True, непустые citations).
**Решения:** офлайн — monkeypatch `rag_demo.embed_texts/get_driver/vector_search_chunks/call_answer_llm` заглушками; HTTP_PROXY-тест не бьёт по живой Ollama (не тратит 2-3 мин) — поднимает свой мини-сервер на 127.0.0.1:0, чем доказывает, что `_NO_PROXY_OPENER` игнорирует прокси структурно, а не потому что цель — localhost:11434; SEARCH-004/005 в логе проверяются чтением `logs/pipeline/<run_id>.jsonl` (по образцу A-04).
**Найденные баги:** нет — код `rag_demo.py` соответствует контракту, паспорту и инварианту №6 по всем проверенным путям (нормальный путь, 0 чанков, сбой LLM, сбой Neo4j, сбой эмбеддинга, парсинг content/reasoning, прокси).
**Прогон:** `.venv/bin/python -m pytest tests/search/ -v` → 27/27 зелёных (12 старых A-04 + 15 новых M-01, включая live-смоук — оба стенда были подняты, скипов не было; полный прогон ~70 с); `lint_precomments.py` — ок.
**Вердикт:** ПРОШЁЛ.

## 2026-07-04 12:20 · module-dev · A-11
**Сделано:** `search/retrieval.py` — `retrieve(driver, intent, question, *, execute_fn=None, top_k, logger=None)`: execute_fn (граф, зона A-10) + векторный топ-K (переиспользован `rag_demo.vector_search_chunks`/`embeddings.embed_texts`), слияние chunk_id графа и вектора (граф первым, дедуп, ≤ `MAX_CONTEXT_CHUNKS=12`), `fetch_chunks_by_ids` — тексты/метаданные для граф-чанков и провенанса contradiction_pairs (title/year/quote). `search/answer.py` — `answer_question(question, *, route_fn=None, execute_fn=None, top_k, run_id=None)`: роутер (ленивый импорт `router.route`) → retrieve → синтез (`rag_demo.call_answer_llm`, ретрай ×1 упрощённым промптом, затем экстрактивная деградация) → `contracts.Answer` (citations, contradictions У-3 из contradiction_pairs, subgraph_node_ids). CLI `python -m ariadna.search.answer "вопрос"`.
**Решения:** ленивый импорт `router.route`/`graph.templates.execute_intent` внутри функций + инъекция параметром — до приземления A-10 честно уходит в `rag_fallback` (SEARCH-001), после — граф-ветка (проверено оба состояния на живом стенде: до и после появления `router.py`/`templates.py`); ANSWER_BACKEND=anthropic — заглушка-деградация (Claude API отключён PM), не реализовывал заново клиент; новые коды SEARCH-006 (execute_fn упал) и SEARCH-007 (синтез недоступен дважды/anthropic-заглушка → экстрактивная выжимка) в ERRORS.md.
**Проблемы:** нет — lint/impорт/оба живых пути (vector-only и graph+vector) прошли с первого прогона; 3 нарушения `lint_precomments.py` относятся к файлам зоны A-10 (`router.py`, `templates.py`), не трогал.
**Живой смоук (все 3 через `answer_question`, стенд Neo4j+Ollama поднят):** до приземления A-10 — вопрос №2 (католит/электроэкстракция никеля): rag_fallback, retrieve 0.84с, synthesis 34.4с, found=True, citations=8, contradictions=0; вопрос №1 (обессоливание воды): rag_fallback, synthesis 156.5с, found=True, citations=8, contradictions=0 (честно: «прямого ответа под заданные параметры нет», указаны мембранные технологии). После приземления A-10 — вопрос №4 (закачка шахтных вод): template_id=`mine_water_injection`, retrieve 1.43с (граф+вектор), synthesis 42.0с, found=True, citations=12, node_ids=30, **1 contradiction** (обратный осмос vs мембранная дистилляция, citation с реальным quote) — граф-ветка отработала end-to-end. Все 3 Answer прошли `Answer.model_validate`.
**Открыто:** синтез 35–160с на вопрос (в рамках щедрого таймаута 240с из M-01, ретрай не понадобился ни разу в смоуке); ретрай/экстрактивная деградация (SEARCH-007) не покрыты живым смоуком (Ollama не падала) — тестеру стоит замокать `AnswerLLMError` на обеих попытках.

## 2026-07-04 · module-dev (Sonnet) · A-10
**Сделано:** `search/router.py` — `route(question)` детерминированный (regex по темам RU/EN, без LLM): 4 темы -> template_id + `extraction.rules.extract_constraints` (числа, переиспользован) + год (диапазон/«последние N лет»/«с YYYY») + гео/`compare_geography` (маркеры «в России и за рубежом»/«мировая практика»/«отечественн»/«сравни») + слоты (канон через `graph.ontology`). `graph/cypher_templates.py` (только строки TEMPLATES, вынесен из-за лимита ~350 строк) + `graph/templates.py` — `execute_intent(driver, intent)`: 6 шаблонов (`desalination_methods/catholyte_circulation/experiments_publications_by_topic/mine_water_injection/compare_ru_foreign/gap_matrix`), только `$param` (инвариант №4); каждый шаблон возвращает `node_ids`/`chunk_ids` в строке — execute_intent просто объединяет; `get_contradiction_pairs` — CONTRADICTS между узлами подграфа. Неизвестный template_id/gap_matrix без слотов -> `ValueError`.
**Решения:** живой прогон на боевом Neo4j показал, что многие узлы-темы (обессоливание, шахтные воды) НЕ имеют прямых графовых связей к нужным Process (данные сущностного извлечения разрежены) — ослабил матчинг: constraints фильтруются ПОСЛЕ сбора (list-проекция, не WHERE внутри OPTIONAL MATCH — не режет строки), `mine_water_injection` матчит Process по имени ИЛИ через `EXISTS{}` со-упоминание в чанке с материалом темы (Neo4j 5.26 — subquery работает), `experiments_publications_by_topic` — со-упоминание Material/Experiment-Publication в общем чанке (прямых DESCRIBED_IN мало). Код SEARCH-002 из постановки задачи занят («слот не заполнен»); SEARCH-006/007 заняты параллельным A-11 — зарегистрировал **SEARCH-008** (неизвестный template_id).
**Проблемы:** нет — lint (`lint_precomments.py`), `pytest tests/search/ tests/graph/ -q` (27+116 зелёных, без регрессий) прошли с первого прогона после декомпозиции templates.py.
**Живой смоук (`python -m ariadna.graph.templates "вопрос"`, боевой Neo4j):**
| Запрос жюри | template_id | rows | node_ids | chunk_ids | contradiction_pairs |
|---|---|---|---|---|---|
| №1 обессоливание | desalination_methods | 30 | 1084 | 891 | 46 |
| №2 католит/электроэкстракция | catholyte_circulation | 11 | 13 | 274 | 0 |
| №3 Au/Ag/МПГ штейн-шлак, 5 лет | experiments_publications_by_topic | 30 | 30 | 19 | 0 |
| №4 закачка шахтных вод, RU+зарубеж | mine_water_injection (compare_geography=True) | 30 | 30 | 699 | 1 |
Плюс отдельно проверены `compare_ru_foreign` (topic=обессоливание: 4 rows/6 chunk_ids) и `gap_matrix` (material=штейн,process=флотация: 30 rows/18 chunk_ids) — вне 4 эталонных, для A-12/A-11.
**Открыто:** гео-признак большинства узлов/документов — `unknown` (наблюдение A-09 по всему корпусу, не блокирует шаблоны — `doc_geographies`/`node_geography` в выдаче есть, но по факту почти всегда `unknown`); A-11 уже проверил интеграцию (см. запись выше) — `router.route`/`templates.execute_intent` встроены в `answer_question` через ленивые импорты, вопрос №4 дал 1 contradiction через граф-ветку.

## 2026-07-04 · module-tester · A-10/A-11
**Сделано:** 55 тестов, 4 новых файла: `tests/search/test_router.py` (27 — 4 эталонных вопроса → template_id, extract_constraints reuse, годы, compare_geography, канонизация слотов, rag_fallback+SEARCH-001, QueryIntent.model_validate, CLI-смоук subprocess), `tests/graph/test_templates.py` (13, живой Neo4j только чтение — 6 шаблонов, rows>0 для 4 эталонных, форма результата, только $param, SEARCH-008, guard gap_matrix), `tests/search/test_retrieval.py` (10 — слияние граф/вектор, дедуп, cap MAX_CONTEXT_CHUNKS, SEARCH-006 деградация, rag_fallback, fetch_chunks_by_ids офлайн+живой смоук, 1 живой смоук retrieve() с реальным embed_texts), `tests/search/test_answer.py` (5, LLM только замокан — happy path/citations/contradictions/subgraph, инвариант №6, экстрактивная деградация SEARCH-007, anthropic-заглушка, route_fn сбой). Никакого живого answer-LLM синтеза не вызывалось.
**Найденные баги:** 1) `router._detect_slot_value` матчит канон/синоним ЛИТЕРАЛЬНОЙ подстрокой без учёта морфологии — для дословных вопросов №1/№4 жюри слот `process` (запрос №1, «обессоливания» ≠ «обессоливание») и оба слота запроса №4 («закачки шахтных вод»/«шахтных вод» ≠ канонам в им.падеже) остаются пустыми, хотя template_id определяется верно (отдельный permissive-regex со стемом); execute_intent не падает (дефолты TEMPLATE_DEFAULT_CANONICALS), но QueryIntent.slots не отражает реальный вопрос — средняя серьёзность. 2) `router._detect_geography`: `_COMPARE_MARKERS_RE` дословно содержит те же подстроки `зарубежн\w*`/`отечественн\w*`, что и `_ONLY_FOREIGN_RE`/`_ONLY_RU_RE` → ветки «гео-фильтр без сравнения» (geography=FOREIGN/RU, compare=False; router.py:196-199) — недостижимый код: любой маркер сразу даёт compare_geography=True. 3) Дословный текст запроса №2 жюри («…в мировой практике…», предложный падеж) не матчит `мировая\s+практик` (только им.падеж) → compare_geography=False для самого запроса №2 вопреки смыслу формулировки. Тесты фиксируют фактическое (баговое) поведение с докстрингом-объяснением, не подгоняют ожидания под баг.
**Проблемы:** подтверждена (не мной внесена — воспроизведена на СТАРЫХ файлах M-01/A-04 тоже) чувствительная к порядку коллизия `tests/search/ tests/graph/` при передаче ДВУХ путей одной командой (`from conftest import …` в test_embeddings.py/test_rag_demo.py/test_retrieval.py — plain-import, не pytest-фикстура): `pytest tests/graph/ tests/search/` проходит, `pytest tests/search/ tests/graph/` падает ImportError на «cannot import name … from 'conftest'». Безопасно: гонять каталоги раздельно ЛИБО одной командой `pytest tests/` (весь путь, без коллизии).
**Прогоны:** `tests/search/ -q` → 68/68 (1 намеренно deselected — живой ответ-LLM смоук M-01, ресурсное ограничение параллельного смоука оркестратора); `tests/graph/ -q` → 129/129 (116 старых + 13 новых); `tests/ -q` (весь проект, ОДНОЙ командой) → 505 passed, 2 deselected (тот же живой answer-LLM смоук + `tests/extraction/test_llm_extract.py::test_extract_chunk_live_smoke`, тоже живой LLM — тем же резервом), 3 xfailed (пред. известные границы A-07, не мои). `scripts/lint_precomments.py` — ок.
**Открыто:** баги 1-3 выше — в router.py (зона A-10, код не правил по правилам задачи), рекомендация в review: стемминг/лемматизация слотов + развести регексы `_COMPARE_MARKERS_RE`/`_ONLY_FOREIGN_RE`/`_ONLY_RU_RE` + расширить «мировая практика» на падежные формы.

## 2026-07-04 14:15 · module-dev fixer · A-10/A-11
**Сделано:** №1 крит. (retrieval.py) — граф-чанки ранжируются по косинусу к вопросу (переиспользован question_vec; embedding кандидатов батчем через fetch_chunks_by_ids, косинус в чистом Python, кандидатов ≤ ~900), квоты слияния: граф ≤ 7 / вектор ≥ 5 / дедуп / добор до 12 (`_rank_graph_chunks_by_relevance`, `_merge_with_quota`). №2 (router) — слоты по основам слов (`_stem_word`/`_term_pattern` + lru_cache): Q1/Q4 жюри заполняют слоты канонами. №3 — гео-регексы разведены: «в зарубежной/отечественной практике» → FOREIGN/RU, compare=False (ветки были мёртвым кодом). №4 — `миров\w*\s+практик` матчит падежи: Q2 жюри → compare_geography=True.
**Решения:** эмбеддинг вопроса упал → граф в исходном порядке + SEARCH-004 (деградация, не падение); test_retrieval.py переписан под новую семантику (15 тестов: ранжирование, квоты, дедуп через vector-score, деградация без вектора), в test_router.py 5 «баговых» тестов tester'а переписаны на правильное поведение + 2 новых гео-кейса (29).
**Смоук 4 запросов жюри (живой стенд, `python -m ariadna.search.answer`):** Q1 desalination_methods — содержательный (ионный обмен, мембраны и др.), 12 cit/0 contr, 138.4с; Q2 catholyte_circulation — содержательный (Nikkelverk/Niihama, схемы, скорости), 12/0, 192.5с; Q3 experiments_publications_by_topic — содержательный (ОИП-06-2018, финские работы), 12/0, 417.9с; Q4 mine_water_injection — честная оговорка + смежные методы с цитатами (пруды-накопители, повторное использование), 12/**1 contradiction**, 109.7с. Критерий «≥3 из 4 содержательных» выполнен.
**Проблемы:** Q4 — в корпусе нет чанков именно про закачку в глубокие горизонты (3 векторные пробы дали те же документы очистки/сверхглубокой добычи) — пробел данных, не ранжирования; синтез Q3 долгий (418с, reasoning) — к сведению для демо.
**Прогоны:** `pytest tests/ -q` (единый вызов) → 514 passed, 3 xfailed, 0 failed; `lint_precomments.py` — ок.
**Открыто:** заполненный слот сузил граф-выдачу Q1 (node_ids 1084→4: шаблон разворачивает синонимы только явного канона, не всех дефолтов) — ответ содержательный за счёт ранжирования+квот; расширение graph-охвата (слот ∪ дефолты) — на усмотрение оркестратора.
